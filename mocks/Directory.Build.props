<Project>
    <!--
        Моки — самодостаточные минимальные сервисы (собираются в Docker из
        собственной папки). Пустой файл останавливает наследование корневого
        Directory.Build.props, чтобы на них не навешивались анализаторы.
    -->
</Project>
